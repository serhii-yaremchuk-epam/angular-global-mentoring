import { FreshIndicatorDirective } from './fresh-indicator.directive';

describe('FreshIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new FreshIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
